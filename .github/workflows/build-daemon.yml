name: Build RFCore Daemon Module

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build-module:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Android NDK (r25c)
      id: setup-ndk
      run: |
        export NDK_PATH=$ANDROID_HOME/ndk/25.2.9519653
        if [ ! -d "$NDK_PATH" ]; then
          wget -q https://dl.google.com/android/repository/android-ndk-r25c-linux.zip
          unzip -q android-ndk-r25c-linux.zip
          NDK_PATH=$PWD/android-ndk-r25c
        fi
        echo "NDK_PATH=$NDK_PATH" >> $GITHUB_ENV

    - name: Compile C++ Daemon (ARM64)
      run: |
        # 定位交叉编译器
        TOOLCHAIN=$NDK_PATH/toolchains/llvm/prebuilt/linux-x86_64
        CXX=$TOOLCHAIN/bin/aarch64-linux-android29-clang++

        # 创建编译输出目录
        mkdir -p build_out

        # 暴力编译：将 daemon (可能还有 interfaces) 下的代码编译为二进制
        # 注意：如果你的 interfaces 里有 .cpp 文件，也会一并编译进去
        $CXX -O2 -static-libstdc++ -Wall rfcore/daemon/*.cpp -o build_out/rfcore_daemon
        
        echo "Daemon 二进制编译完成！"

    - name: Assemble Magisk/KernelSU Module
      run: |
        # 1. 创建刷机包的基础系统级目录
        mkdir -p module/system/bin
        
        # 2. 将你自己的 Magisk 模板文件全部拷贝过来 (包括 module.prop, post-fs-data.sh 等)
        cp -r rfcore/Magisk_module/* module/
        
        # 3. 将你的 SELinux 策略规则拷贝到模块根目录，适配 KernelSU/Magisk
        cp -r rfcore/sepolicy/* module/ 2>/dev/null || true
        
        # 4. 放入新鲜出炉的 Daemon 二进制文件并赋予执行权限
        cp build_out/rfcore_daemon module/system/bin/
        chmod +x module/system/bin/rfcore_daemon
        
        # 5. 确保脚本拥有执行权限 (防丢掉权限)
        chmod +x module/*.sh 2>/dev/null || true

        # 6. 最终封包打包
        cd module
        zip -r9 ../RFCore-Module-Release.zip ./*

    - name: Upload Module Artifact
      uses: actions/upload-artifact@v4
      with:
        name: RFCore-Module-ZIP
        path: RFCore-Module-Release.zip
